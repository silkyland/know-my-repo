# tiny-node-api

A minimal Express API used as a know-my-repo eval fixture. Two resources
(`users`, `orders`) deliberately repeat the same conventions so a real
convention-extraction pass has evidence to find.
